package com.bayviewglen.dayfive;

public class NotesOnStack {
	
	// stack is first in last out
	// queue is first in first out
	
	public static void main(String[] args) {
		//row1, col1 1-filled
		//row2, col1 (conflict) --> move to row2 , col2 (conflict) --> move to row2, col3 == :)
		//stack is good for counting brackets and polynomials.
		
		// given text file. ensure it is HTML.
		
		// push is add
		// pop is removing
		// peek look at top
		// empty see if empty
	}

}
